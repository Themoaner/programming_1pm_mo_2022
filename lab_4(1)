#include <iostream>

int main() {
	int n;
	std::cin >> n;
	int mas[10005];
	bool flag = true;

	for (int i = 0; i < n; i++)
	{
		std::cin >> mas[i];
		bool isPrime = true;
		for (int d = 2; d <= sqrt(mas[i]); d++)
		{
			if (mas[i] % d == 0)
			{
				isPrime = false;
				break;
			}
		}
		if (isPrime) flag = false;
	}

	if (flag == true)
	{
		for (int i = 0; i < n - 1; i++)
		{
			for (int j = i + 1; j < n; j++)
			{
				if (mas[i] > mas[j])
				{
					std::swap(mas[i], mas[j]);
				}
			}
		}
	}

	for (int i = 0; i < n; i++)
	{
		std::cout << mas[i] << std::endl;
	}

	system("pause");
	return 0;
}
